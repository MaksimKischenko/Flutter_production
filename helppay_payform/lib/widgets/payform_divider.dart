
import 'package:flutter/material.dart';

class PayFormDivider extends StatelessWidget {
  const PayFormDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Color(0xffEBEBEB),
      height: 2,
    );
  }
}